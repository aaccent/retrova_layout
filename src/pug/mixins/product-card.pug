mixin Product-card(checked=false, dataPath='')
  article.product-card.product-card--hovered.product-card--stock.product-card--new(data-article="")&attributes(attributes)
    .product-card__body
      .product-card__image
        .product-card__picture.product-card__picture--active(data-image="1")
          +imgOriginal('products/pic-91.jpg', 'retrova product image')
        .product-card__picture(data-image="2")
          +imgOriginal('products/pic-92.jpg', 'retrova product image')
        .product-card__picture(data-image="3")
          +imgOriginal('products/pic-93.jpg', 'retrova product image')
        .product-card__picture(data-image="4")
          +imgOriginal('products/pic-91.jpg', 'retrova product image')

        .product-card__hover-mask
          .product-card__hover-item(data-hover="1")
          .product-card__hover-item(data-hover="2")
          .product-card__hover-item(data-hover="3")
          .product-card__hover-item(data-hover="4")

        .product-card__stickers
          span.product-card__sticker.sticker.sticker--new
            +icon-mono('star')
            span
              | new

          span.product-card__sticker.sticker.sticker--sale
            +icon-mono('star')
            span
              | sale

          span.product-card__sticker.sticker.sticker--sold-out
            +icon-mono('star')
            span
              | нет в наличии

        .product-card__favorites.favorites
          label.favorites__label
            input.favorites__input(
              hidden,
              type="checkbox",
              name="favorite",
              value="",
              id="",
              checked=checked
            )
            span.favorites__icon
              +icon-mono('heart')

        .product-card__cart.cart
          label.cart__label(data-path=(dataPath ? dataPath : ''))
            input.cart__input(
              hidden,
              type="checkbox",
              name="cart",
              value="",
              id=""
            )
            span.cart__icon
              +icon-mono('cart')

      .product-card__info
        a.product-card__name(href="#")
          | Джинсовая куртка
        .product-card__price
          span.product-card__price-value.product-card__price-value--actual
            | 25 000₽
          span.product-card__price-value.product-card__price-value--old
            | 32 000₽
