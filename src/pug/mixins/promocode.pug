mixin Promocode(id,name,placeholder)
  // form.promocode.promocode--not-valid(action="")
  // form.promocode.promocode--valid(action="")
  form.promocode.promocode(action="")
    input.promocode__input(
      id= id,
      required,
      name= name,
      type="text",
      placeholder= placeholder,
      autocomplete="off"
    )
    button.promocode__button(type="submit")
      +icon-multi('arrow-right')

    span.promocode__error
      | Ошибка
    span.promocode__tooltip
      | Промокод применен
