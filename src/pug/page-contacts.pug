extends layout/secondary.pug

block variables
  - const bodyClass = "page-home preload"
  - const headerClass = "header-main header--secondary"
  - const pageTitle = "Контакты"
  - const siteLang = "ru"
  - const breadcrumbs = ['Главная', 'Контакты']

block content
  include pages/page-contacts/main.pug
